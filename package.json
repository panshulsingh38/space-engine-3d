{
  "name": "space-engine-3d",
  "version": "1.0.0",
  "description": "Realistic 3D Space Simulator and orbital dynamics engine.",
  "main": "electron/main.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "electron:dev": "electron .",
    "electron:build": "vite build && electron-builder",
    "android:sync": "vite build && npx cap sync",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@capacitor/android": "^5.6.0",
    "@capacitor/cli": "^5.6.0",
    "@capacitor/core": "^5.6.0",
    "electron": "^28.1.0",
    "gh-pages": "^6.3.0",
    "vite": "^5.0.10"
  }
}
